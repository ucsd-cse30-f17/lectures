#include <stdlib.h>
#include <stdio.h>

#include <stdio.h>

int main(int argc, char** args) {
  int i;
    for(i = 0; i < argc; i += 1) {
        printf("%s\n", args[i]);
          }
            return 0;
            }
